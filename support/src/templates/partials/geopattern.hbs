<style>
  .jumbotron {
    {{#if options.gradient}}{{gradient options.gradient}}{{else}}
    background-image: {{geopattern (default @page.geopattern @page.title)}};
    {{/if}}
  }
  .{{@site.content}} h1,
  .{{@site.content}} h2,
  .{{@site.content}} h3,
  .{{@site.content}} h4 {
    color: {{geoColor (default @page.geopattern @page.title)}};
  }
</style>